name: Keycloak Redeploy

on:
  workflow_dispatch:
  workflow_call:

jobs:
  redeploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Render Keycloak Deploy
        run: curl -X POST ${{ secrets.RENDER_KEYCLOAK_HOOK }}